def reverse_multiply(input: list[int]) -> list[int]:
    for item in input:
        item *= 2
    output: list[int] = []
    i: int = 1
    while i <= len(input):
        output.append(input[len(input) - i])
    return output

x: reverse_multiply([1, 2, 3])
print(x)